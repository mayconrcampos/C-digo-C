#include <stdio.h>
#include <string.h>

int main(){
    char momes[100];
    int valores[100];

    int indice, conta, opcao;
    float total, valor;

    printf("-=-= Armazém do seu Zé - Caderno do Fiado! -=-=\n");
    printf("-----------------------------------------------\n");
    printf("1. Cadastrar Cliente e Nota\n");
    printf("-------------------------------------------------\n");
    scanf("%d", &opcao);

    switch (opcao){
        case 1:
            conta = 0;
            printf("-=-= Cadastrando Nova Conta -=-=\n");
            printf("Digite o primeiro nome do Cliente: ");
            gets(nomes);
            for(indice=0; indice<=100; indice++){
                if(nomes[])
            }
            break;
    
        default:
            break;
    }

    return 0;
}